{
  "name": "test-master-vercel",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && rm -rf public && mkdir -p public && cp -rf frontend/dist/* public/"
  }
}
